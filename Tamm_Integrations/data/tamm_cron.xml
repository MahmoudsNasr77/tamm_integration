<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_cron_sync_tamm_data" model="ir.cron">
        <field name="name">Tamm: Sync Vehicle Data</field>
        <field name="model_id" ref="fleet.model_fleet_vehicle"/>
        <field name="state">code</field>
        <field name="code">model.search([('tamm_vehicle_id', '!=', False)]).sync_with_tamm()</field>
        <field name="interval_number">15</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
        <field name="user_id" ref="base.user_admin"/>
    </record>

    <record id="ir_cron_check_maintenance" model="ir.cron">
        <field name="name">Tamm: Check Due Maintenance</field>
        <field name="model_id" ref="model_tamm_maintenance"/>
        <field name="state">code</field>
        <field name="code">model._cron_check_due_maintenance()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active">True</field>
        <field name="user_id" ref="base.user_admin"/>
    </record>
</odoo>
